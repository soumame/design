---
const { title, description, icon, link } = Astro.props;
---

<div
  class="bg-sky-700 backdrop-blur-xl rounded-3xl p-2.5 md:p-3 flex w-full h-full justify-between items-center relative"
>
  <span class="text-white">
    <h3 class="md:text-xl text-lg font-bold">{title}</h3>
    <p class="md:text-sm text-xs text-gray-200">{description}</p>
  </span>
  <slot />
  <a href={link} class="absolute top-0 left-0 w-full h-full"></a>
</div>
